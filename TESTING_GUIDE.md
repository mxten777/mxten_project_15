# 🧪 최소 안전망 테스트 가이드

## [A] 왜 이 테스트만으로 충분한지

### 테스트 철학
> **"테스트는 안전망이지, 완벽함의 증명이 아니다"**

이 프로젝트는 **3~4개의 핵심 테스트**로 다음을 보장합니다:

### 1️⃣ 스모크 테스트 (01-smoke.test.tsx)
- **목적**: 앱이 치명적 에러 없이 시작되는지
- **보호 범위**: 
  - 의존성 오류
  - 초기화 실패
  - 렌더링 크래시
- **실패 시 의미**: "배포하면 사이트가 안 뜬다"

### 2️⃣ 데이터 방어 테스트 (02-data-defense.test.tsx)
- **목적**: 백엔드 장애/빈 데이터 상황에서도 앱이 버티는지
- **보호 범위**:
  - 빈 배열 처리
  - undefined/null 방어
  - Fallback UI 작동
- **실패 시 의미**: "실서비스에서 API 장애 시 화면 깨짐"

### 3️⃣ 핵심 UX 테스트 (03-critical-ux.test.tsx)
- **목적**: 사용자가 반드시 해야 하는 행동이 가능한지
- **보호 범위**:
  - CTA 버튼 존재
  - 링크 작동
  - 핵심 흐름 유지
- **실패 시 의미**: "사용자가 문의/포트폴리오 볼 수 없음 = 비즈니스 실패"

### 4️⃣ Navbar 테스트 (04-navbar.test.tsx) - 선택
- **목적**: 페이지 간 이동 가능
- **보호 범위**: 네비게이션 기본 동작
- **실패 시 의미**: "사이트 탐색 불가"

---

## [B] 테스트 파일 구조

```
src/
├── test/
│   ├── setup.ts                    # 테스트 환경 설정 (1회)
│   ├── 01-smoke.test.tsx           # 필수 ⭐⭐⭐
│   ├── 02-data-defense.test.tsx    # 필수 ⭐⭐⭐
│   ├── 03-critical-ux.test.tsx     # 필수 ⭐⭐⭐
│   └── 04-navbar.test.tsx          # 선택 ⭐
│
├── pages/
│   └── LandingPage.tsx
├── components/
│   └── Navbar.tsx
└── data/
    └── portfolio.ts

vitest.config.ts                    # Vitest 설정
```

### 파일 이름 규칙
- `01-`, `02-` 등 번호로 실행 순서 명시
- 목적이 명확한 이름 사용
- `.test.tsx` 확장자로 테스트 파일 식별

---

## [C] 실제 테스트 코드

### 📦 설치
```bash
npm install --save-dev vitest @vitest/ui happy-dom
npm install --save-dev @testing-library/react @testing-library/jest-dom @testing-library/user-event
```

### ▶️ 실행
```bash
# 전체 테스트 실행
npm test

# Watch 모드 (개발 중)
npm run test:watch

# UI 모드 (시각적 확인)
npm run test:ui
```

### 테스트 파일 상세

#### 1. setup.ts
```typescript
// Framer Motion 비활성화 (속도 향상)
// IntersectionObserver mock
// Console.error → 테스트 실패 처리
```

#### 2. 01-smoke.test.tsx
```typescript
✅ 랜딩 페이지가 오류 없이 마운트된다
✅ Hero 영역이 존재한다
✅ 메인 헤딩이 존재한다
```

#### 3. 02-data-defense.test.tsx
```typescript
✅ 포트폴리오 데이터가 비어있어도 렌더링된다
✅ 카테고리 데이터가 없어도 UI가 깨지지 않는다
✅ 빈 데이터 상태에서도 치명적 에러가 없다
```

#### 4. 03-critical-ux.test.tsx
```typescript
✅ 메인 CTA 버튼이 존재한다
✅ CTA 버튼이 올바른 경로로 링크된다
✅ 포트폴리오 보기 링크가 존재한다
✅ 핵심 링크들이 클릭 가능하다
```

#### 5. 04-navbar.test.tsx (선택)
```typescript
✅ Navbar가 렌더링된다
✅ 로고/홈 링크가 존재한다
✅ 주요 네비게이션 링크들이 존재한다
```

---

## [D] 이 이상 테스트를 하지 않아도 되는 이유

> **"배포 전에 반드시 확인해야 하는 최소한만 테스트했고, 나머지는 TypeScript와 ESLint가 막아준다."**

### 왜 더 안 만드는가?

#### ❌ 하지 않는 것들
1. **Snapshot 테스트**: UI 변경 시 계속 업데이트 필요 → 유지보수 부담
2. **CSS 검증**: TailwindCSS + 디자인 QA로 충분
3. **애니메이션 테스트**: 사용자 경험은 수동 테스트가 낫다
4. **모든 컴포넌트 단위 테스트**: 통합 테스트가 더 실용적
5. **E2E 테스트**: 이 규모에서는 과하다

#### ✅ 대신 의존하는 것들
- **TypeScript**: 타입 에러 컴파일 타임에 잡음
- **ESLint**: 코드 품질 자동 검증
- **Vitest Fast Check**: 3~4개 테스트가 5초 안에 끝남
- **수동 QA**: 디자인/UX는 사람이 봐야 함

### 테스트 성능 목표
- ✅ 전체 실행 시간: **< 5초**
- ✅ 테스트 수: **3~4개**
- ✅ 커버하는 범위: **치명적 에러만**
- ✅ 유지보수 부담: **거의 없음**

### 신규 기능 추가 시
```typescript
// ❌ 새 테스트 추가할 필요 없는 경우
- 새 컴포넌트 추가 (기존 페이지에 포함)
- 스타일 변경
- 애니메이션 추가
- 텍스트 수정

// ✅ 새 테스트 추가해야 하는 경우
- 새로운 페이지 라우트 추가 → 스모크 테스트 1개
- 새로운 핵심 CTA 추가 → UX 테스트 1개
- 새로운 데이터 소스 추가 → 방어 테스트 1개
```

---

## 📊 예상 결과

```bash
$ npm test

 ✓ src/test/01-smoke.test.tsx (3)
   ✓ 스모크 테스트: 랜딩 페이지 기본 렌더링
     ✓ 랜딩 페이지가 오류 없이 마운트된다
     ✓ Hero 영역이 존재한다
     ✓ 메인 헤딩이 존재한다

 ✓ src/test/02-data-defense.test.tsx (3)
   ✓ 데이터 방어 테스트: 빈 데이터 처리
     ✓ 포트폴리오 데이터가 비어있어도 렌더링된다
     ✓ 카테고리 데이터가 없어도 UI가 깨지지 않는다
     ✓ 빈 데이터 상태에서도 치명적 에러가 없다

 ✓ src/test/03-critical-ux.test.tsx (4)
   ✓ 핵심 UX 테스트: 주요 사용자 흐름
     ✓ 메인 CTA 버튼이 존재한다
     ✓ CTA 버튼이 올바른 경로로 링크된다
     ✓ 포트폴리오 보기 링크가 존재한다
     ✓ 핵심 링크들이 클릭 가능하다

 ✓ src/test/04-navbar.test.tsx (3)
   ✓ 선택 테스트: Navbar 기본 동작
     ✓ Navbar가 렌더링된다
     ✓ 로고/홈 링크가 존재한다
     ✓ 주요 네비게이션 링크들이 존재한다

Test Files  4 passed (4)
     Tests  13 passed (13)
  Start at  14:30:00
  Duration  2.3s (transform 500ms, setup 200ms, collect 800ms, tests 400ms)
```

---

## 🎯 핵심 원칙

1. **"이 테스트가 실패하면 배포하면 안 된다"** 라고 말할 수 있는 것만 테스트
2. **구현이 바뀌어도 깨지지 않는 테스트** (UI 디테일 검증 금지)
3. **5초 이내 실행** (빠른 피드백 루프)
4. **테스트 자체가 유지보수 부담이 되지 않도록**

---

## ✅ 이 테스트로 보장하는 것

- ✅ 사이트가 로드된다
- ✅ 데이터 장애 시에도 크래시하지 않는다
- ✅ 사용자가 핵심 행동을 할 수 있다
- ✅ 네비게이션이 작동한다

## ❌ 이 테스트로 보장하지 않는 것

- ❌ UI가 아름답다 (디자이너 QA)
- ❌ 애니메이션이 부드럽다 (수동 테스트)
- ❌ 모든 엣지 케이스 (TypeScript가 막음)
- ❌ 성능 최적화 (Lighthouse)

---

**결론**: 이 4개의 테스트는 "최소한의 안전망"이며, 이 이상은 **비용 대비 효과가 떨어진다**.
