# 바이브 코딩 MVP 프로젝트 네이밍 규칙

> **버전**: 1.0  
> **최종 수정일**: 2026-01-22  
> **적용 범위**: 83개 전체 MVP 프로젝트

---

## 📋 목차
1. [프로젝트 식별자 구조](#1-프로젝트-식별자-구조)
2. [네이밍 규칙 상세](#2-네이밍-규칙-상세)
3. [ID 체계 규칙](#3-id-체계-규칙)
4. [Slug 생성 규칙](#4-slug-생성-규칙)
5. [파일명 규칙](#5-파일명-규칙)
6. [중복 방지 규칙](#6-중복-방지-규칙)
7. [실무 적용 예시](#7-실무-적용-예시)

---

## 1. 프로젝트 식별자 구조

### 1.1 필수 식별자 3종
모든 MVP 프로젝트는 다음 3가지 식별자를 필수로 가집니다:

| 식별자 | 용도 | 예시 | 규칙 |
|--------|------|------|------|
| **title** | 화면 표시용 (한글) | `한국환경안전연구소 공식 홈페이지` | 한글, 최대 50자 |
| **id** | 내부 데이터 식별 | `ketri-01` | 영문 소문자 + 숫자 + 하이픈 |
| **slug** | URL 경로용 | `ketri-homepage` | 영문 소문자 + 하이픈만 |

### 1.2 식별자 간 관계
```
title (한글 표시명)
  ↓
id (내부 식별자) → 데이터베이스 키, API 참조
  ↓
slug (URL 경로) → SEO 최적화, 사용자 친화적 URL
  ↓
thumbnail (파일명) → {slug}.webp 또는 {id}.webp
```

---

## 2. 네이밍 규칙 상세

### 2.1 한글 표시명 (title)

**원칙:**
- 고객사/프로젝트의 정식 명칭 사용
- 불필요한 수식어 제거
- 일관된 형태 유지

**올바른 예시:**
```
✅ 한국환경안전연구소 공식 홈페이지
✅ 바이칼시스템즈 홈페이지
✅ 디비인포 리뉴얼
✅ Vibe Office Hub
✅ 도서관 예약 시스템
```

**잘못된 예시:**
```
❌ [최신] 한국환경안전연구소 홈페이지 (수식어 불필요)
❌ 바이칼 시스템즈 (띄어쓰기 불일치)
❌ DB인포 리뉴얼 (영문 약어 혼용)
❌ 도서관 예약 시스템 Ver2.0 (버전 정보 불필요)
```

### 2.2 프로젝트 카테고리별 명칭 가이드

| 카테고리 | 명칭 패턴 | 예시 |
|----------|-----------|------|
| 기업 홈페이지 | `{기업명} 홈페이지` | 바이칼시스템즈 홈페이지 |
| 공공기관 | `{기관명} 공식 홈페이지` | 한국환경안전연구소 공식 홈페이지 |
| 플랫폼/서비스 | `{서비스명}` (있는 그대로) | Vibe Office Hub |
| 예약/시스템 | `{대상} {기능} 시스템` | 도서관 예약 시스템 |
| 리뉴얼 | `{기업명} 리뉴얼` | 디비인포 리뉴얼 |

---

## 3. ID 체계 규칙

### 3.1 ID 구조
```
{prefix}-{number}
```

### 3.2 Prefix 종류 및 용도

| Prefix | 용도 | 예시 | 프로젝트 유형 |
|--------|------|------|---------------|
| **mvp** | 초기 MVP 프로젝트 | `mvp-04`, `mvp-26` | 기업 홈페이지, 기본 웹사이트 |
| **gen** | 범용 생성형 프로젝트 | `gen-01`, `gen-30` | SaaS, 플랫폼, 자동화 시스템 |
| **jdx** | AI/교육 프로젝트 | `jdx-01`, `jdx-50` | 교육 플랫폼, AI 기반 서비스 |
| **grok** | 공공/예약 프로젝트 | `grok-57`, `grok-70` | 예약 시스템, 공공 서비스 |
| **ketri** | KETRI 관련 프로젝트 | `ketri-01`, `ketri-10` | 환경안전연구소 시리즈 |
| **baikal** | 바이칼 자체 프로젝트 | `baikal-01`, `baikal-50` | 자체 서비스, 문화 플랫폼 |
| **mxten** | MX텐코딩 내부 프로젝트 | `mxten-06`, `mxten-15` | 내부 시스템, 포트폴리오 |
| **new** | 신규 프로젝트 | `new-02`, `new-40` | 최근 추가 프로젝트 |
| **pub** | 공공/행정 프로젝트 | `pub-01`, `pub-03` | 공공기관, 행정 서비스 |
| **music** | 문화/예술 프로젝트 | `music-01`, `music-02` | 음악, 예술 관련 |
| **figma** | 디자인 시스템 프로젝트 | `figma-01`, `figma-02` | UI/UX 디자인 시스템 |
| **bank** | 금융 프로젝트 | `bank-01` | 은행, 금융 서비스 |
| **fuel** | 물류/에너지 프로젝트 | `fuel-05` | 물류, 에너지 관련 |

### 3.3 번호 부여 규칙
- **01-10**: 초기/핵심 프로젝트
- **11-20**: 확장 프로젝트
- **21-30**: 변형/추가 프로젝트
- **31-50**: 시범/테스트 프로젝트
- **51-99**: 특수 목적 프로젝트

### 3.4 ID 중복 검증
```javascript
// ID 중복 검증 규칙
const isValidProjectId = (id) => {
  // 1. 형식 검증: prefix-number
  const pattern = /^[a-z]+-\d{1,2}$/;
  if (!pattern.test(id)) return false;
  
  // 2. 중복 검증
  const existingIds = portfolioProjects.map(p => p.id);
  if (existingIds.includes(id)) return false;
  
  // 3. Prefix 유효성 검증
  const validPrefixes = ['mvp', 'gen', 'jdx', 'grok', 'ketri', 'baikal', 'mxten', 'new', 'pub', 'music', 'figma', 'bank', 'fuel'];
  const prefix = id.split('-')[0];
  if (!validPrefixes.includes(prefix)) return false;
  
  return true;
};
```

---

## 4. Slug 생성 규칙

### 4.1 Slug 기본 원칙
- **영문 소문자만 사용**
- **단어 구분은 하이픈(-) 사용**
- **한글을 영문으로 의역**
- **최대 50자 제한**
- **SEO 최적화 고려**

### 4.2 Slug 생성 패턴

**패턴 1: 기업명-타입**
```
{company-name}-{type}

예시:
baical-systems (바이칼시스템즈 → baical systems)
dbinfo-renewal (디비인포 리뉴얼)
korea-copron (한국코프론)
```

**패턴 2: 서비스명 직접 사용**
```
{service-name}

예시:
vibe-office-hub (Vibe Office Hub)
vibe-finance-hub (Vibe Finance Hub)
literature-platform (문학 플랫폼)
```

**패턴 3: 기관명-타입**
```
{organization}-{type}

예시:
ketri-homepage (한국환경안전연구소 공식 홈페이지)
gwangyeon-driving (광연자동차전문학원)
```

**패턴 4: 기능-타입**
```
{function}-{type}

예시:
library-reservation (도서관 예약 시스템)
ai-education-platform (AI 기반 맞춤형 교육 플랫폼)
worker-education-platform (직장인을 위한 AI 교육 플랫폼)
```

### 4.3 Slug 생성 가이드라인

| 한글 표현 | 영문 Slug | 비고 |
|-----------|-----------|------|
| 홈페이지 | `-homepage` 또는 `-website` | 기업명 뒤에 추가 |
| 리뉴얼 | `-renewal` | 리뉴얼 프로젝트 |
| 플랫폼 | `-platform` | 서비스 플랫폼 |
| 시스템 | `-system` | 관리/예약 시스템 |
| 웹앱 | `-webapp` | 웹 애플리케이션 |
| 관리자 | `-admin` | 관리자 페이지 |
| 공식 | 생략 가능 | `ketri-homepage` (공식 생략) |

### 4.4 Slug 중복 방지
```javascript
// Slug 생성 및 중복 검증 함수
const generateSlug = (title) => {
  // 1. 기본 slug 생성
  let slug = title
    .toLowerCase()
    .replace(/[^a-z0-9가-힣\s-]/g, '') // 특수문자 제거
    .replace(/\s+/g, '-') // 공백을 하이픈으로
    .replace(/-+/g, '-') // 연속 하이픈 제거
    .replace(/^-|-$/g, ''); // 앞뒤 하이픈 제거
  
  // 2. 중복 검사
  const existingSlugs = portfolioProjects.map(p => p.slug);
  if (existingSlugs.includes(slug)) {
    // 중복 시 숫자 추가
    let counter = 2;
    while (existingSlugs.includes(`${slug}-${counter}`)) {
      counter++;
    }
    slug = `${slug}-${counter}`;
  }
  
  return slug;
};
```

---

## 5. 파일명 규칙

### 5.1 썸네일 파일명 구조
```
{slug}.webp
또는
{id}.webp (레거시 호환)
```

### 5.2 파일명 우선순위
1. **신규 프로젝트**: `{slug}.webp` 사용 권장
2. **기존 프로젝트**: 기존 파일명 유지 가능 (단, 마이그레이션 권장)
3. **Featured 프로젝트**: 반드시 `{slug}.webp` 형식 사용

### 5.3 파일명 예시

| 프로젝트 | ID | Slug | 권장 파일명 | 레거시 파일명 |
|---------|-------|------|-------------|---------------|
| 한국환경안전연구소 | ketri-01 | ketri-homepage | `ketri-homepage.webp` | `ketri-01.png` |
| Vibe Office Hub | gen-01 | vibe-office-hub | `vibe-office-hub.webp` | `gen-01.png` |
| 도서관 예약 시스템 | grok-57 | library-reservation | `library-reservation.webp` | `grok-57.png` |

### 5.4 스크린샷 파일명 구조
```
{slug}-screenshot-{number}.webp

예시:
ketri-homepage-screenshot-01.webp
ketri-homepage-screenshot-02.webp
vibe-office-hub-screenshot-01.webp
```

---

## 6. 중복 방지 규칙

### 6.1 ID 중복 방지 체크리스트
- [ ] 전체 프로젝트 목록에서 ID 검색
- [ ] prefix-number 조합이 고유한지 확인
- [ ] 동일 prefix 내 번호 순차 확인

### 6.2 Slug 중복 방지 체크리스트
- [ ] 전체 프로젝트 목록에서 slug 검색
- [ ] URL 경로 충돌 여부 확인
- [ ] SEO 키워드 중복 확인

### 6.3 자동 검증 스크립트
```typescript
// portfolio.ts에 추가할 검증 함수
export const validateProjectNaming = () => {
  const ids = portfolioProjects.map(p => p.id);
  const slugs = portfolioProjects.map(p => p.slug);
  
  // ID 중복 검사
  const duplicateIds = ids.filter((id, index) => ids.indexOf(id) !== index);
  if (duplicateIds.length > 0) {
    console.error('중복된 ID:', duplicateIds);
  }
  
  // Slug 중복 검사
  const duplicateSlugs = slugs.filter((slug, index) => slugs.indexOf(slug) !== index);
  if (duplicateSlugs.length > 0) {
    console.error('중복된 Slug:', duplicateSlugs);
  }
  
  // ID 형식 검증
  const invalidIds = ids.filter(id => !/^[a-z]+-\d{1,2}$/.test(id));
  if (invalidIds.length > 0) {
    console.error('잘못된 ID 형식:', invalidIds);
  }
  
  // Slug 형식 검증
  const invalidSlugs = slugs.filter(slug => !/^[a-z0-9]+(-[a-z0-9]+)*$/.test(slug));
  if (invalidSlugs.length > 0) {
    console.error('잘못된 Slug 형식:', invalidSlugs);
  }
  
  return {
    isValid: duplicateIds.length === 0 && duplicateSlugs.length === 0 && invalidIds.length === 0 && invalidSlugs.length === 0,
    errors: { duplicateIds, duplicateSlugs, invalidIds, invalidSlugs }
  };
};
```

---

## 7. 실무 적용 예시

### 7.1 신규 프로젝트 등록 프로세스

**Step 1: 프로젝트 정보 수집**
```
고객사명: 서울대학교병원
프로젝트 유형: 예약 시스템
카테고리: 공공·예약·문화
```

**Step 2: 식별자 생성**
```typescript
{
  id: 'grok-80',           // 예약 시스템이므로 grok prefix
  title: '서울대학교병원 진료 예약 시스템',
  slug: 'snuh-appointment-system',  // Seoul National University Hospital
  category: '공공·예약·문화',
  thumbnail: '/images/snuh-appointment-system.webp',
  // ... 기타 정보
}
```

**Step 3: 중복 검증**
```bash
# ID 중복 확인
grep "id: 'grok-80'" src/data/portfolio.ts

# Slug 중복 확인
grep "slug: 'snuh-appointment-system'" src/data/portfolio.ts
```

**Step 4: 파일 생성**
```bash
# 썸네일 파일 생성
/public/images/snuh-appointment-system.webp

# 스크린샷 파일 생성
/public/images/snuh-appointment-system-screenshot-01.webp
/public/images/snuh-appointment-system-screenshot-02.webp
```

### 7.2 기존 프로젝트 마이그레이션 예시

**Before (레거시):**
```typescript
{
  id: 'new-40',
  title: '도서관 예약 시스템',
  slug: 'library-reservation',
  thumbnail: '/images/20251014_new_40.png',  // ❌ 날짜 기반 파일명
}
```

**After (표준):**
```typescript
{
  id: 'grok-57',  // 예약 시스템에 맞는 prefix로 변경
  title: '도서관 예약 시스템',
  slug: 'library-reservation',
  thumbnail: '/images/library-reservation.webp',  // ✅ slug 기반 파일명
}
```

---

## 📊 네이밍 규칙 Quick Reference

| 구분 | 규칙 | 예시 | 금지 사항 |
|------|------|------|-----------|
| **ID** | `prefix-number` | `ketri-01` | 대문자, 공백, 특수문자 |
| **Slug** | `word-word` | `ketri-homepage` | 대문자, 밑줄, 한글 |
| **파일명** | `{slug}.webp` | `ketri-homepage.webp` | 공백, 한글, 특수문자 |
| **Title** | 자유형식 (50자 이내) | `한국환경안전연구소 공식 홈페이지` | 과도한 수식어 |

---

## ✅ 체크리스트

### 신규 프로젝트 등록 시
- [ ] ID가 `prefix-number` 형식인가?
- [ ] ID가 전체 프로젝트에서 고유한가?
- [ ] Slug가 영문 소문자 + 하이픈만 사용했는가?
- [ ] Slug가 전체 프로젝트에서 고유한가?
- [ ] 썸네일 파일명이 slug와 일치하는가?
- [ ] 파일 확장자가 .webp인가?
- [ ] Title이 명확하고 간결한가?

### 기존 프로젝트 수정 시
- [ ] 기존 ID를 유지하는가? (변경 시 영향도 분석 필수)
- [ ] Slug 변경 시 리다이렉트 설정했는가?
- [ ] 썸네일 파일이 실제로 존재하는가?
- [ ] 레거시 파일과 신규 파일이 중복 생성되지 않았는가?

---

**문서 관리:**
- 작성자: 바이브 코딩 개발팀
- 승인자: 프로젝트 매니저
- 다음 리뷰 예정일: 2026-04-22
